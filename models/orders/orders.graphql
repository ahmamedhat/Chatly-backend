type Query {
  orders: [Order]
  order(_id: ID!): Order
}

type Mutation {
  makeNewOrder(name: String!, email: String!, total: Float!): Order!
}

type Order {
  _id: ID!
  total: Float!
  email: String!
  name: String!
  createdAt: String!
  updatedAt: String!
}
