type Query {
  users: [User]
  user(_id: ID!): User
}

type Mutation {
  createNewUser(name: String!, email: String!): User!
}

type User {
  _id: ID!
  email: String!
  name: String!
  chats: [Chat]
  createdAt: String!
  updatedAt: String!
}
